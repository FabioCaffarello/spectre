{{- if .Values.playwrightAdapter.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "spectre.fullname" . }}-playwright-adapter
  labels:
    {{- include "spectre.labels" . | nindent 4 }}
    app.kubernetes.io/component: playwright-adapter
spec:
  replicas: {{ .Values.playwrightAdapter.replicas }}
  selector:
    matchLabels:
      {{- include "spectre.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: playwright-adapter
  template:
    metadata:
      {{- with .Values.playwrightAdapter.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "spectre.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: playwright-adapter
        {{- with .Values.playwrightAdapter.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- range . }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      {{- with .Values.playwrightAdapter.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.playwrightAdapter.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.playwrightAdapter.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- include "spectre.tlsVolumes" (dict "slot" "playwright-adapter" "context" .) | nindent 6 }}
      containers:
        - name: playwright-adapter
          image: {{ include "spectre.image" (dict "service" .Values.playwrightAdapter "context" .) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: {{ .Values.playwrightAdapter.service.port }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.observability.metricsPort }}
              protocol: TCP
          env:
            - name: SPECTRE_ADAPTER_GRPC_PORT
              value: {{ .Values.playwrightAdapter.service.port | quote }}
            {{- include "spectre.commonEnv" . | nindent 12 }}
            {{- include "spectre.observabilityEnv" . | nindent 12 }}
            {{- include "spectre.tlsEnv" . | nindent 12 }}
            {{- with .Values.playwrightAdapter.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- include "spectre.tlsVolumeMounts" . | nindent 10 }}
          # W3.4 mTLS posture: kubelet's `grpc:` probe doesn't
          # present a client certificate, so the adapter would
          # reject it at the TLS handshake. Same conditional shape
          # the engine uses (W3.3). The adapter's TLS listener
          # binds late in startup, so TCP being open is a reliable
          # readiness proxy.
          {{- if .Values.certManager.enabled }}
          readinessProbe:
            tcpSocket:
              port: {{ .Values.playwrightAdapter.service.port }}
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            tcpSocket:
              port: {{ .Values.playwrightAdapter.service.port }}
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 5
          {{- else }}
          readinessProbe:
            {{- toYaml .Values.playwrightAdapter.probes.readiness | nindent 12 }}
          livenessProbe:
            {{- toYaml .Values.playwrightAdapter.probes.liveness | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.playwrightAdapter.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "spectre.fullname" . }}-playwright-adapter
  labels:
    {{- include "spectre.labels" . | nindent 4 }}
    app.kubernetes.io/component: playwright-adapter
spec:
  type: {{ .Values.playwrightAdapter.service.type }}
  ports:
    - port: {{ .Values.playwrightAdapter.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: {{ .Values.observability.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "spectre.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: playwright-adapter
{{- end }}
